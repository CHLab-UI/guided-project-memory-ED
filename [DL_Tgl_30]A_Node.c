#include <stdio.h>

/** 🚀🚀[Teori]🚀🚀
 * sebuah node adalah setiap elemen dari linked list yang mengandung data
 * dan pointer ke node berikutnya
 * Pembuatan node pada bahasa c dengan menggunakan struct
 *
 * [statusEmpty, panjang, next] -> [statusEmpty, panjang, next] -> [statusEmpty, panjang, next]
 * satu [statusEmpty, panjang, next] merupakan 1 node,
 */

/**
 * 🙂🙂[Instruksi]🙂🙂
 * A1. Buatlah struct node yang memiliki 3 field, yaitu statusEmpty, panjang, dan next
 */

// typedef struct node{
//     int statusEmpty;
//     int panjang;
//     struct node* next;
// } Node;

/**
 * 🙂🙂[Instruksi]🙂🙂
 * A2. Buatlah typedef untuk Node* disini, hal ini akan memudahkan pembuatan program kedepannya
 */

// typedef Node* NodePtr;


int main(){

   return 0;
}